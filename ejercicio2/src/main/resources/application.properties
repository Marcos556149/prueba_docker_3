spring.application.name=ejercicio2
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/spring_boot_jpa}
spring.datasource.username=${DB_USER_NAME:postgres}
spring.datasource.password=${DB_USER_PASSWORD:43642127}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
server.port=${PORT:8080}